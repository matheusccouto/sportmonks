name: Probabilities

on:
  push:
    branches:
      - main
  schedule:
    - cron: * * * * *

permissions:
  contents: read
  id-token: write

jobs:
  atletas:
    uses: ./.github/workflows/etl.yml
    with:
      endpoint: football/predictions/probabilities
      filter: .data
      table: probabilities